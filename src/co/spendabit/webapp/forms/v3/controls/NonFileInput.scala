package co.spendabit.webapp.forms.v3.controls

//import org.apache.commons.fileupload.FileItem

/** Any control which will be submitted as one or more string-based parameters. That is, pretty
  * much everything other than file-upload fields (<input type="file" />).
  */
//abstract class NonFileInput[T] extends Control[T] {
//
//  def validate(s: String): Either[String, T]
//}
